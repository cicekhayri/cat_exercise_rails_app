<h1><%= @cat.name %></h1>
<p>
<%= image_tag @cat.picture, width: 200 %>
</p>
<p>Is born: <%= @cat.birth_date.strftime("%Y %d %b") %></p>
<p>Owner:<br/>
<%= @cat.owner.nil? ? "No Owner" : @cat.owner.name %>
</p>

<p>
<%= link_to "Delete Image", delete_cat_image_path, method: :delete, data: {confirm: "Are you sure" } %>
<%= link_to "Back", :back %>
