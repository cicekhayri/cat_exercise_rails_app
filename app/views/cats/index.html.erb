<h1>Cats</h1>
<div class="cats-list">
  <ul>
  <% @cats.each do |cat| %>
    <li><%= cat.name %> 
      <span class='birth-date'>
        <%= cat.birth_date.strftime("%Y %d %b") if cat.birth_date %>
      </span>
    </li>
  <% end %>
  </ul>
</div>

